const colors = require("./colors");

module.exports = {
    disabled:false,
    classList:"px-5 py-2 select-none outline-none",
    color:colors.lightBg,
    textColor: colors.primary,
    borderColor: colors.primary,
    ringColor: colors.primary,
    type: "submit",
    borderRadius: "xl",
    growSize: false,
    transitionDuration: 500
}